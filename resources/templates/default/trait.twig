{% if model.namespace %}namespace {{ class.namespace }};{% endif %}
{% if not model.requiredFiles.isEmpty %}{{ include('partials/required_files_partial.twig') }}{% endif %}
{% if not model.useStatements.isEmpty %}{{ include('partials/use_statement_partial.twig') }}{% endif %}
{% if config.generateDocblock %}{{ include('partials/docblock_partial.twig', {indentation: 0}) }}{% endif %}
trait {{ model.name }} {
{% for trait in model.traits %}
	{%- if loop.first %}

	{%~ endif %}
	use {{ trait }};
{% endfor %}
{% for property in model.properties %}

{{ include('property.twig', {model: property}) }}
{% endfor %}
{% for method in model.methods %}

{{ include('method.twig', {model: property}) }}
{% endfor %}
}
