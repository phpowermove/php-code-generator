{% if config.generateDocblock %}{{ include('partials/docblock_partial.twig', {indentation: 1}) }}{% endif %}
    {{ model.visibility }}{% if model.isStatic %} static{% endif %}
{%- if config.generatePropertyTypes and model|type != '' %} {{ model|type }}{% endif %} ${{ model.name }}
{%- if model.hasValue %} = {{ model|value }}{% endif %};